const add = (a, b) => {
  return a + b;
};

const name = 'Pramod';

const sub = (a, b) => {
  return a - b;
};

// module.exports.add = add;
// module.exports.sub = sub;
// module.exports.name = name;

module.exports = { add, sub, name };
